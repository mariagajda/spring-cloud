server:
  port: 8761
eureka:
  instance:
    prefer-ip-address: true # powoduje preferowanie adresów ip zamiast domen (może rozwiązywać problem występujący na windows z Docker)
  client:
    should-enforce-registration-at-init: false
