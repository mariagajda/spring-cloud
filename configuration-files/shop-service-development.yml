spring:
  cloud:
    openfeign:
      client:
        config:
          PAYMENTS-SERVICE: #default:
            logger-level: headers
logging:
  level:
    #org.springframework.web.client.RestTemplate: DEBUG
    pl.training: DEBUG

