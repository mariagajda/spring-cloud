spring:
  cloud:
    openfeign:
      client:
        config:
          PAYMENTS-SERVICE: #default:
            logger-level: basic
logging:
  level:
    #org.springframework.web.client.RestTemplate: DEBUG
    pl.training: DEBUG

